# Placeholder model files
# These are generated by running the training scripts in ml-service/train/
# Run: python train/train_crop.py  →  generates crop_model.pkl
# Run: python train/train_yield.py →  generates yield_model.pkl
# Run: python train/train_price.py →  generates price_model.pkl
# Run: python train/train_irrigation.py → generates irrigation_model.pkl
